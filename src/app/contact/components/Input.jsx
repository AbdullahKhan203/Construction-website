import React from 'react'

export default function Input(props) {
  return (
    <>
    <input className={props.stylee} type={props.type} width={props.width}  placeholder={props.placeholder} name="" id="" />
    </>
  )
}
